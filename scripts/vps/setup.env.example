# ═══════════════════════════════════════════════════════════════════════
# setup.env -- Configuration for setup_all.sh
# Copy this file and fill in your values before running the setup script.
# Usage: bash setup_all.sh --env /path/to/setup.env
# ═══════════════════════════════════════════════════════════════════════

# ── GitHub Repos ──
GITHUB_HARNESS_REPO=global-mysterysnailrevolution/harness
GITHUB_DASHBOARD_REPO=global-mysterysnailrevolution/openclaw-dashboard

# ── Paths ──
HARNESS_DIR=/opt/harness
DASHBOARD_DIR=/opt/openclaw-dashboard
OPENCLAW_DIR=/docker/openclaw-kx9d

# ── OpenClaw Container ──
OPENCLAW_CONTAINER=openclaw-kx9d-openclaw-1

# ── Dashboard ──
DASHBOARD_PORT=7000
# Auto-generated if blank:
DASHBOARD_TOKEN=

# ── Tailscale (optional, for unattended setup) ──
# Get a key from https://login.tailscale.com/admin/settings/keys
TAILSCALE_AUTH_KEY=

# ── API Keys (written to OPENCLAW_DIR/.env) ──
# These are required for OpenClaw to function:
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# ── Optional ──
# WANDB_API_KEY=        # For Weave tracing
# TOOLHIVE_SECRET_KEY=  # For ToolHive gateway
# HLOS_API_KEY=         # For HLOS.ai integration
